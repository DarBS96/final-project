// import express from "express";
// const routerAuth = express.Router();
// import { authenticateToken } from "../middleware/auth.js";

// routerAuth.post("/", authenticateToken);

// export default routerAuth;
